# Required for automatic speaker identification / diarization.
# Without these, the app falls back to Chrome Web Speech, which transcribes but cannot identify speakers automatically.
VITE_AZURE_SPEECH_KEY=
VITE_AZURE_SPEECH_REGION=
