# Required for automatic speaker identification / diarization in local development.
# Uses Deepgram Nova live transcription with diarization.
VITE_DEEPGRAM_API_KEY=

# Without this key, the app falls back to Chrome Web Speech,
# which transcribes but cannot identify speakers automatically.
