# CaptionTable

CaptionTable is a small accessible live-caption interface. It includes large caption modes, a high-contrast theme, keyboard-friendly controls, persistent notices, and screen-reader status regions.

Open `index.html` in a browser or serve the folder with a static web server:

```bash
python3 -m http.server 8000
```
